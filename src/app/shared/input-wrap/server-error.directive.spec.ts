import { ServerErrorDirective } from './server-error.directive';

describe('ServerErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new ServerErrorDirective();
    expect(directive).toBeTruthy();
  });
});
